# Retired, 4 September 2026

This folder built the Play Store app as a Trusted Web Activity (Chrome showing
phaserugbymanager.com) for every upload up to version code 15. The Play app is
now the Capacitor shell in `packaging/android/`, same package name, same
signing key, version codes continuing from 16.

Kept for the record of what shipped and for the version-code history in
`twa-manifest.json`. Do not build from it again.
