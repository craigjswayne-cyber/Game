Drop real club logo files in here as <clubId>.png (e.g. leicester.png,
toulouse.png) and the game will use them automatically instead of the
generated crests. Club ids are the slugs in src/data/leagues/*.ts.
