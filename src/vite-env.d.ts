/// <reference types="vite/client" />

/**
 * Stamped in by vite.config.ts at build time: version, UTC build date and the
 * short commit. Shown in small grey type at the foot of the title screen so a
 * phone can say which build it is running.
 */
declare const __BUILD_TAG__: string
